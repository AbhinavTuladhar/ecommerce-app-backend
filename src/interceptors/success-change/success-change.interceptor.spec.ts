import { SuccessChangeInterceptor } from './success-change.interceptor';

describe('SuccessChangeInterceptor', () => {
  it('should be defined', () => {
    expect(new SuccessChangeInterceptor()).toBeDefined();
  });
});
